# Wipro-TalentNext-Java--Training
#number of primes in specified range
#include<stdio.h>
int k=2;
   int d=input1,i,c=0;
   int p=0;
   int cou=0;
   while(d<=input2){
       for(i=2;i<d;i++){
           if(d%i==0){
               c++;
           }
       }
       if(c==0){
           cou++;
       }
       d++;
       c=0;
   }
   return cou;
   
   
   
   #All digits count
import java.io*;
import java.util.*;
class UserMainCode
{
   public int allDigitsCount(int input1)
{
   String str=Integer.toString(input1);
   return str.length();
  }
